@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <div class="sidebar-header">
            <h1>📁 BlazorFileManager</h1>
            <p class="subtitle">Test & Demo</p>
        </div>

        <nav class="nav-menu">
            <NavLink class="nav-item" href="/" Match="NavLinkMatch.All">
                <span class="nav-icon">🏠</span>
                <span class="nav-text">Home</span>
            </NavLink>

            <NavLink class="nav-item" href="/file-manager-demo">
                <span class="nav-icon">📤</span>
                <span class="nav-text">File Manager Demo</span>
            </NavLink>

            <NavLink class="nav-item" href="/storage-demo">
                <span class="nav-icon">💾</span>
                <span class="nav-text">Storage Demo</span>
            </NavLink>

            <NavLink class="nav-item" href="/manual-upload-demo">
                <span class="nav-icon">⏸️</span>
                <span class="nav-text">Manual Upload</span>
            </NavLink>

            <NavLink class="nav-item" href="/multilanguage-demo">
                <span class="nav-icon">🌍</span>
                <span class="nav-text">Multi-Language</span>
            </NavLink>

            <NavLink class="nav-item" href="/examples">
                <span class="nav-icon">📋</span>
                <span class="nav-text">Beispiele</span>
            </NavLink>

            <NavLink class="nav-item" href="/documentation">
                <span class="nav-icon">📖</span>
                <span class="nav-text">Dokumentation</span>
            </NavLink>
        </nav>

        <div class="sidebar-footer">
            <p>Version 1.0.0</p>
            <p>© 2024</p>
        </div>
    </div>

    <main class="main-content">
        <div class="content-wrapper">
            @Body
        </div>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

<style>
    * {
        box-sizing: border-box;
    }

    body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        background-color: #f5f7fa;
    }

    .page {
        display: flex;
        height: 100vh;
        overflow: hidden;
    }

    .sidebar {
        width: 280px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        display: flex;
        flex-direction: column;
        box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .sidebar-header {
        padding: 2rem 1.5rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }

    .sidebar-header h1 {
        margin: 0;
        font-size: 1.5rem;
        font-weight: 700;
        color: white;
    }

    .subtitle {
        margin: 0.5rem 0 0;
        font-size: 0.9rem;
        opacity: 0.9;
        color: rgba(255, 255, 255, 0.9);
    }

    .nav-menu {
        flex: 1;
        padding: 1rem 0;
        overflow-y: auto;
    }

    .nav-item {
        display: flex;
        align-items: center;
        padding: 0.875rem 1.5rem;
        color: rgba(255, 255, 255, 0.9);
        text-decoration: none;
        transition: all 0.2s ease;
        border-left: 3px solid transparent;
        cursor: pointer;
    }

    .nav-item:hover {
        background-color: rgba(255, 255, 255, 0.1);
        color: white;
    }

    .nav-item.active {
        background-color: rgba(255, 255, 255, 0.15);
        border-left-color: white;
        color: white;
        font-weight: 600;
    }

    .nav-icon {
        font-size: 1.25rem;
        margin-right: 0.75rem;
        min-width: 1.5rem;
        text-align: center;
    }

    .nav-text {
        font-size: 0.95rem;
    }

    .sidebar-footer {
        padding: 1rem 1.5rem;
        border-top: 1px solid rgba(255, 255, 255, 0.2);
        font-size: 0.75rem;
        opacity: 0.7;
        text-align: center;
    }

    .sidebar-footer p {
        margin: 0.25rem 0;
    }

    .main-content {
        flex: 1;
        overflow-y: auto;
        background-color: #f5f7fa;
    }

    .content-wrapper {
        max-width: 1400px;
        margin: 0 auto;
        padding: 2rem;
    }

    #blazor-error-ui {
        background: lightyellow;
        bottom: 0;
        box-shadow: 0 -1px 5px rgba(0, 0, 0, 0.2);
        display: none;
        left: 0;
        padding: 0.6rem 1.25rem 0.7rem 1.25rem;
        position: fixed;
        width: 100%;
        z-index: 1000;
    }

    #blazor-error-ui .dismiss {
        cursor: pointer;
        position: absolute;
        right: 0.75rem;
        top: 0.5rem;
    }

    /* Responsive Design */
    @@media (max-width: 768px) {
        .sidebar {
            width: 100%;
            height: auto;
            position: relative;
        }

        .page {
            flex-direction: column;
        }

        .main-content {
            flex: 1;
        }

        .sidebar-header h1 {
            font-size: 1.25rem;
        }

        .content-wrapper {
            padding: 1rem;
        }
    }

    /* Scrollbar Styling */
    .nav-menu::-webkit-scrollbar {
        width: 6px;
    }

    .nav-menu::-webkit-scrollbar-track {
        background: rgba(255, 255, 255, 0.1);
    }

    .nav-menu::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.3);
        border-radius: 3px;
    }

    .nav-menu::-webkit-scrollbar-thumb:hover {
        background: rgba(255, 255, 255, 0.5);
    }
</style>
